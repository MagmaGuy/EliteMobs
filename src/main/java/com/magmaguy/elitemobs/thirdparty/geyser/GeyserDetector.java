package com.magmaguy.elitemobs.thirdparty.geyser;

import org.bukkit.entity.Player;

public class GeyserDetector {

    public static boolean bedrockPlayer(Player player) {
        //This is where I would detect if this is a bedrock player if Geyser didn't require 1.16 and actually had an all-in-one way of letting me know if it's a bedrock client
        //maintained because hope is the last one to die, will be replaced with something that works... some day
        return false;
    }
}
