package com.magmaguy.elitemobs.mobconstructor;

/** Independent reasons that can keep one Elite actor's power runtimes paused. */
public enum ElitePowerPauseReason {
    INTERRUPT,
    ROOT,
    MIND_SERVICE
}
